# Cloud sync (optional). Without these the app runs localStorage-only.
# Add both in Vercel (Project Settings -> Environment Variables), then redeploy.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
